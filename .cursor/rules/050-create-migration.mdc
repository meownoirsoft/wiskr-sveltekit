---
description: Generate SQL migration in /migrations only
globs:
alwaysApply: true
---
  Create a SQL migration file inside the  project root `migrations` folder, with a filename starting with a UTC timestamp. Do not place this in libs or root directly.

  When you include code like auth.ui in migrations you should instead use (SELECT auth.id as uid) so that we don't have performance issues later.
